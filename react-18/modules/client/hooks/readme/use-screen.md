# use-screen

Tracks dimensions and breakpoint/device flags with zero dimensions outside a browser. This source export currently lacks the Beyond bundle marker; public `/hooks` import availability is not established. The breakpoint object is recreated each render and is an effect dependency.

See the repository-local [hooks contract](../../../../../docs/hooks.md) for exact version differences, source links and integration requirements.
