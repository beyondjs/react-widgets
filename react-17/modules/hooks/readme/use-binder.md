# use-binder

Subscribes objects to one or more events through useEffect and removes those same subscriptions on cleanup. The effect captures initial arguments; later object, event or callback changes do not rebind. Supply valid event-capable objects for the selected adapter version.

See the repository-local [hooks contract](../../../../docs/hooks.md) for exact version differences, source links and integration requirements.
