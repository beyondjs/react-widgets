# use-texts

Returns `[ready, texts]` from Kernel CurrentTexts, optionally selecting a key. The effect captures initial specifier/key. Current cleanup incorrectly calls on instead of off and does not destroy the model; this implementation must not be described as leak-free.

See the repository-local [hooks contract](../../../../docs/hooks.md) for exact version differences, source links and integration requirements.
